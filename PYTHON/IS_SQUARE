import math 
def is_square(n):
    while (n>=0):
        # Find floating point value of  
        # square root of n. 
        sr = math.sqrt(n) 
   
        # If square root is an integer 
        return ((sr - math.floor(sr)) == 0) 
    return False
